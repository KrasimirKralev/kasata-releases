# Kasata releases

Release channel for the Kasata household expense app. The app reads
`version.json` to detect updates and downloads the APK from the matching
GitHub release. Source code lives in a private repository.
